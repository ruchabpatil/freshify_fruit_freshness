[project]
name = "freshify-fruit-freshness"
version = "1.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "tensorflow",
    "numpy",
    "pillow"
]

[tool.vercel]
entrypoint = "api.predict:handler"
